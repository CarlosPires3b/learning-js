const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // se for igual a 0 é par se for igual a 1 é impar
-divisao // operador unário


console.log(soma, subtracao, multiplicacao, divisao, modulo)